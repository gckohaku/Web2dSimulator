// @ts-check

import { GraphicBase } from "../GraphicBase.d.js";

/**
 * @implements {GraphicBase}
 */
class GraphicBouncingCircle {
	

	/**
	 * 初期化
	 * @param {CanvasRenderingContext2D} context 
	 * @returns {number}
	 */
	initialize(context) {

		return 1;
	}

	/**
	 * 更新
	 * @param {CanvasRenderingContext2D} context 
	 * @returns {number}
	 */
	update(context) {

		return 1;
	}

	/**
	 * 描画
	 * @param {CanvasRenderingContext2D} context 
	 * @returns {number}
	 */
	draw(context) {

		return 1;
	}

	/**
	 * 後処理
	 * @param {CanvasRenderingContext2D} context 
	 * @returns {number}
	 */
	release(context) {

		return 1;
	}
}